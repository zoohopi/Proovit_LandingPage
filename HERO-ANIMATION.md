# 히어로 연출

두 랜딩 파일의 첫 화면은 `hero-stage.js`의 `proovit-hero` 컴포넌트를 공유합니다. 기존 디자인 컴포넌트 런타임의 `x-import` 방식으로 로드합니다.

## 재생 순서

- 0–0.9초: 배경 페이드인.
- 0.9–2.95초: ○ △ □의 선이 그려지며 끝점에서 핑크 불꽃 입자 발생.
- 2.95–3.55초: 완성된 도형 유지, 명함 바탕 등장.
- 3.55–5.35초: 도형을 실제 명함 좌표로 축소·이동. 프루비, 상단 메뉴, 별똥별 동시 페이드인.
- 5.55초부터: 한글 음절 단위로 95ms 간격 타이핑.
- 8초 이후: 다음 섹션 링크 표시. 최종 구도 유지, 좌우 별똥별만 은은하게 반복.

`assets/hero-background.png`는 01 배경, `assets/hero-proovie.png`는 04의 투명 캐릭터 원본입니다. 도형, 대사, 내비게이션과 별 효과는 웹에서 별도 렌더링합니다. 최종 합성 시안의 글자나 메뉴를 이미지로 굽지 않았습니다.

소리는 사용자가 ‘소리 켜기’를 누른 뒤부터 적용됩니다. 완료 후 켠 경우 ‘다시 보기’로 타이핑 소리를 들을 수 있습니다. 모션 감소 설정에서는 최종 장면을 즉시 표시하며 소리는 재생하지 않습니다. 숨겨진 탭에서는 애니메이션을 일시 정지합니다.

## 검증

- Node 구문 검사 통과.
- 실제 디자인 컴포넌트 런타임에서 로드 및 최종 장면 확인.
- 데스크톱 및 390×844 모바일에서 캐릭터, 명함 좌표, 글자, 메뉴 여백 확인.
- 초대창 링크와 상단 고정 메뉴 확인.

로컬 미리보기: 프로젝트에서 HTTP 서버를 실행한 뒤 `proovit-landing.dc.html`을 엽니다. 기존 support.js는 React와 글꼴을 CDN에서 로드하므로 처음 접속 시 인터넷 연결이 필요합니다.
