# 초대장 애니메이션

`proovit-landing.dc.html`의 마지막 초대장에 적용했습니다.

- 시작 디자인: `assets/invitation-envelope-closed-v1.png`
- 최종 디자인: `assets/invitation-envelope-concept-v4.png`
- 정지 이미지의 문구를 그대로 겹치지 않고, 봉투·덮개·편지지를 웹 레이어로 재구성했습니다. 참가번호, 발행일, 참가비, 정원과 수락 동작은 기존 DC 데이터와 연결됩니다.
- 초대장 구간에서 스크롤하면 덮개가 먼저 회전하고 편지지가 위로 올라옵니다. 역스크롤하면 다시 닫힙니다. 5차 보간과 135ms 추종 지연으로 가속·감속합니다.
- 기존 마우스 위치를 따라오는 핑크빛, 클릭 시 도장·충격파·흔들림 효과를 유지합니다.
- 아래에서 위로 비추는 네 개의 핑크색 서치라이트가 서로 다른 주기로 교차합니다. 화면 밖 및 비활성 탭에서는 멈춥니다.
- 심볼 원형 크기는 유지하고 내부 심볼을 48%에서 72%로 확대했습니다. 참가번호는 커뮤니티 제목과 같은 Pretendard 700이며, 번호가 올라온 후 250ms 뒤 밑줄이 520ms 동안 그어집니다.
- 용지의 비율을 세로로 늘리고, 종이 가장자리·접힘선·봉투 각 면에 반사광과 음영을 분리했습니다. 재질과 조명 설정은 `invitation-materials.css`에서 조정합니다.
- 모션 감소 설정에서는 편지지를 즉시 펼쳐 표시합니다. 닫힌 편지는 키보드 접근에서 제외하고, 별도의 열기 버튼을 제공합니다.

`invitation-stage.css`에서 색상·레이어·반응형 크기, `invitation-stage.js`에서 스크롤 진행도·개봉 시점을 조정할 수 있습니다. `.invite-scroll`의 높이를 늘리면 개봉에 필요한 스크롤 거리가 길어집니다.

검증: 데스크톱/390px 모바일 표시, 가로 넘침, 수락 문구 변경, 브라우저 오류 로그, 컨트롤러의 개봉/역스크롤/모션 감소/정리 로직을 확인했습니다.

로컬 미리보기: 기존 `미리보기-실행.cmd` 또는 `node tmp/preview-server.cjs`로 실행한 뒤 `http://127.0.0.1:4173/proovit-landing.dc.html#invite`에 접속합니다.
